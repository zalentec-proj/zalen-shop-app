name: Quality gate

on:
  pull_request:
  push:
    branches:
      - main
      - refactor/migrate-to-next

permissions:
  contents: read

jobs:
  application:
    name: Typecheck, tests, build and dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run test:coverage
      - run: npm run build
      - run: npm run security:audit
      - run: node scripts/security/scan-secrets.mjs
